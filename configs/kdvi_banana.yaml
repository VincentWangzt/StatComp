# KDVI on Banana distribution
# MCMC distillation: SGLD transitions + MMD² loss
seed: 42
cuda_visible_devices: "0"
use_cuda: true

runner_type: KDVI
target_type: banana
vi_model_type: ConditionalGaussian

train:
  epochs: 50000
  batch_size: 128
  vi:
    lr: 1.0e-3
    var_lr: 1.0e-3
    scheduler:
      type: StepLR
      step_size: 2000
      gamma: 0.9
  annealing:
    enabled: true
    scheme: linear
    steps: 25000
  kdvi:
    mcmc_type: sgld             # 'sgld' or 'hmc'
    mcmc_steps: 5               # K — number of transition steps
    mcmc_step_size: 0.05        # step size (conservative)
    hmc_leapfrog_steps: 10      # L — leapfrog sub-steps (HMC only)
    kernel: gaussian            # kernel for MMD (from utils/kernels.py registry)
    fit_bandwidth_on: x         # 'x', 'y', 'xy', or 'none'
  grad_clip: 10.0
  log:
    metric_log_freq: 500
    loss_log_freq: 100
    reverse_log_freq: 500
    time_avg_window: 500
  checkpoint:
    enabled: true
    freq: 5000
  sample:
    freq: 5000
    num: 10000
  plot:
    freq: 5000
    num: 10000
  ema:
    enabled: false
    beta: 0.999

metric:
  kl_ite:
    enabled: true
    num_samples: 10000
  w2:
    enabled: true
    num_samples: 10000
    num_projections: 1000
  elbo:
    enabled: true
    batch_size: 512
    num_batches: 10
    num_z_samples: 5000
  fisher:
    enabled: false
    num_samples: 1000
    num_is_samples: 512
  ksd:
    enabled: false
    num_samples: 2000
  mmd:
    enabled: true
    num_samples: 1000
  bnn:
    enabled: false
    num_samples: 500
  expected_log_marginal:
    enabled: false
    num_ref_samples: 1000
    num_model_samples: 50000
    sample_batch_size: 50000
    dim_chunk: 50
    ref_chunk: 500
    model_chunk: 5000
    min_bandwidth: 1.0e-6
    dtype: float32

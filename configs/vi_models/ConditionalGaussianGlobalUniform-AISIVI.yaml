z_dim: ${target.z_dim}
epsilon_dim: ${target.z_dim}
hidden_dim: 128
num_layers: 2
activation: relu
variance_parameterization: softplus_var
global_variance_init: 0.01725
var_min: 1.0e-4
device: ${device}
uniform: true

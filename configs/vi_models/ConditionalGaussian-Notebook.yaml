z_dim: ${target.z_dim}
# Notebook-style: latent_dim independent of target dimensionality.
# Hard-coded literal (not interpolated from target) so we don't fight
# base_runner's z/epsilon-dim override at line 98.
epsilon_dim: 32
hidden_dim: 256
num_layers: 3
activation: silu
device: ${device}
uniform: false # Whether to use uniform prior over epsilon

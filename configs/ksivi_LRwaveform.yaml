# KSIVI on Bayesian Logistic Regression (Waveform dataset, 22-dimensional)
# Original: kernel=gaussian, epochs=200*100=20k, batch=100, lr=0.001, no annealing
seed: 42
cuda_visible_devices: "0"
use_cuda: true

runner_type: KSIVI
target_type: LRwaveform
vi_model_type: ConditionalGaussianGlobal

vi_model:
  epsilon_dim: 10
  hidden_dim: 100
  num_layers: 2
  activation: relu
  variance_parameterization: logvar
  global_log_var_init: -5.0
  global_log_var_min: -20

target:
  data:
    batch_size: null
    batch_mode: full
    source: official_mat
    mat_path: D:/PKU/Programming/StatComp/KSIVI/datasets/waveform.mat

train:
  epochs: 20000
  batch_size: 100
  vi:
    lr: 1.0e-3
    var_lr: 1.0e-3
    betas: [0.9, 0.999]
    scheduler:
      type: StepLR
      step_size: 3000
      gamma: 0.9
  annealing:
    enabled: false
    scheme: linear
    steps: 5000
  ksivi:
    statistic: v
    kernel: gaussian
    detach_kernel: false
    log_p_reg: 0.0
  log:
    metric_log_freq: 1000
    loss_log_freq: 100
    reverse_log_freq: 500
  checkpoint:
    enabled: true
    freq: 5000
  sample:
    freq: 1000
    num: 1000
  plot:
    freq: 5000
    num: 1000

metric:
  kl_ite:
    enabled: false
    num_samples: 10000
  w2:
    enabled: false
    num_samples: 10000
    num_projections: 1000
  elbo:
    enabled: false
    batch_size: 512
    num_batches: 5
    num_z_samples: 200
  ksd:
    enabled: true
    num_samples: 1000
  mmd:
    enabled: false
